# Environment Variables
# Copy this file to .env and fill in your values

# Server
PORT=5000
NODE_ENV=development

# MongoDB — paste your remote MongoDB connection string below
MONGO_URI=mongodb+srv://veronicabridgerton_db_user:D4smFQwDJQ40cWb1@cluster1.xhewf3r.mongodb.net/

# JWT
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=7d

# Email (SMTP) — for password reset emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_email_password
FROM_EMAIL=noreply@mnss.com
FROM_NAME=MNSS Events

# Client URL (for CORS and password reset links)
CLIENT_URL=http://localhost:3000
